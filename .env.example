# MnemoQ environment variables
# Copy to .env and fill in values. Never commit .env.

# Directory containing memory/ with config.json and learnings.jsonl
AGENT_MEMORY_DIR=/path/to/your/project/memory

# API key for authenticated HTTP memory servers (optional, local-only mode doesn't need this)
AGENT_MEMORY_API_KEY=
